[workspace]
members = [
  "constraint-macros",
  "trace-defs",
  "tracer",
]
exclude = ["miden-vm"]

resolver = "2"

# faster tests
[profile.test]
opt-level = 2
# faster tests
[profile.test.package.rand_chacha]
opt-level = 3
