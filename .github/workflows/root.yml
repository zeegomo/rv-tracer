name: CI

on:
  pull_request:
    branches: [ "master" ]

jobs:
  build-and-test:
    uses: ./.github/workflows/rust.yml
  memory-bench:
    uses: ./.github/workflows/mem-bench.yml
  cpu-bench:
    uses: ./.github/workflows/cpu-bench.yml